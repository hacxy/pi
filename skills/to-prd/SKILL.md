---
name: to-prd
description: 把当前对话转化为 PRD 并保存到为md文档 跟踪器——不访谈，只综合你已经讨论过的内容。
disable-model-invocation: true
---

这个 skill 获取当前对话上下文和代码库理解，产出一份 PRD。**不要访谈用户**——只综合你已经知道的内容。

## 流程

1. 探索仓库以了解代码库的当前状态，如果你还没做过的话。整个 PRD 使用项目的领域术语表词汇，并尊重你触及领域的任何 ADR。

2. 勾画出你将要测试该功能的接缝（seams）。现有接缝应优先于新接缝。使用尽可能高的接缝。如果需要新接缝，在你能做到的最高点提出它们。整个代码库中的接缝越少越好——理想数量是一个。

与用户确认这些接缝符合他们的预期。

3. 使用下面的模板编写 PRD，然后保存到项目中`prd/pro-name`。

<prd-template>

## 问题陈述

用户正在面临的问题，从用户的角度。

## 解决方案

问题的解决方案，从用户的角度。

## 用户故事

一份**长**的、编号的用户故事列表。每个用户故事的格式为：

1. 作为 <actor>，我想要 <feature>，以便 <benefit>

<user-story-example>
1. 作为手机银行客户，我想要查看我账户的余额，以便我能对支出做出更明智的决定
</user-story-example>

这份用户故事列表应当极其详尽，并覆盖该功能的各个方面。

## 实现决策

一份已做出的实现决策列表。可以包括：

- 将被构建/修改的模块
- 将被修改的模块接口
- 来自开发者的技术澄清
- 架构决策
- Schema 变更
- API 契约
- 具体的交互

**不要**包含具体的文件路径或代码片段。它们可能很快就会过时。

例外：如果原型产出的一段代码比文字更精确地编码了一个决策（状态机、reducer、schema、类型形状），就在相关决策中内联它，并简要注明它来自原型。裁剪到决策密集的部分——不是可运行的演示，只是重要的部分。

## 测试决策

一份已做出的测试决策列表。包括：

- 什么构成一个好测试的描述（只测试外部行为，不测试实现细节）
- 哪些模块将被测试
- 测试的先例（即代码库中类似的测试类型）

## 范围之外

对本 PRD 而言范围之外的事项描述。

## 补充说明

关于该功能的任何补充说明。

</prd-template>
